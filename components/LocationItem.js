const React = require('react');

var LocationItem = React.createClass({
  render() {
    return (
      <div>
        <h4>Location Item</h4>
      </div>
    );
  }
});

module.exports = LocationItem;
